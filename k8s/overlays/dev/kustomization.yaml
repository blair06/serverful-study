apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: serverful-study-dev

resources:
  - ../../base

patchesStrategicMerge:
  - deployment-patch.yaml

commonLabels:
  environment: dev

images:
  - name: docker.io/$DOCKERHUB_USERNAME/serverful-study
    newTag: develop

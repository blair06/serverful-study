apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: serverful-study-dev

resources:
  - ../../base

patchesStrategicMerge:
  - deployment-patch.yaml

commonLabels:
  environment: dev

images:
  - name: ghcr.io/blair/serverful-study
    newTag: dev
